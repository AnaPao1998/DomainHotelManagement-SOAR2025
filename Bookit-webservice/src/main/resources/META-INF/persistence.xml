<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
             https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

    <!-- switched to local source instead of JTA as it broke the login -->
    <persistence-unit name="bookitPU" transaction-type="RESOURCE_LOCAL">

        <class>ch.unil.bookit.domain.User</class>
        <class>ch.unil.bookit.domain.Guest</class>
        <class>ch.unil.bookit.domain.HotelManager</class>

        <properties>
            <!-- JDBC connection from payara container to db container -->
            <property name="jakarta.persistence.jdbc.driver"
                      value="com.mysql.cj.jdbc.Driver"/>
            <property name="jakarta.persistence.jdbc.url"
                      value="jdbc:mysql://db:3306/bookit_db?useSSL=false&amp;allowPublicKeyRetrieval=true&amp;serverTimezone=UTC"/>
            <property name="jakarta.persistence.jdbc.user"
                      value="bookit"/>
            <property name="jakarta.persistence.jdbc.password"
                      value="pw"/>

            <!-- Schema handling + logging -->
            <property name="jakarta.persistence.schema-generation.database.action"
                      value="create"/>
            <property name="eclipselink.logging.level"
                      value="FINEST"/>
        </properties>
    </persistence-unit>
</persistence>
